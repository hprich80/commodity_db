services:
  postgres:
    image: postgres:15
    container_name: fred_db
    environment:
      POSTGRES_DB: fred_pipeline
      POSTGRES_PASSWORD: password
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
